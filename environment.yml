name: spectral_lib_matcher
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - pandas
  - pip
  - pip:
      - matchms
      - pytest
      - pytest-runner
      - tqdm
  - python <= 3.9.7
  - requests
